# Dog Go M0.5 Day 12 柴犬资产日志

**日期：** 2026-06-28  
**状态：** 已完成，可进入 Day 13

## 已完成

- 依据正式视觉基准生成同一只成年柴犬的四姿态统一母版。
- 冻结 `sit_window`、`lie_rest`、`stand_turn`、`play_bow` 的体型、花纹与光向。
- 建立可复现的色键清理、裁切、底部锚定和 @2x/@3x 导出脚本。
- 四套全身主层已进入 `Assets.xcassets`：`DogSitWindow`、`DogLieRest`、`DogStandTurn`、`DogPlayBow`。
- 每套姿态已补齐阴影、身体、前腿、后腿、尾巴、头部、双耳、睁眼、闭眼和口鼻动作层。
- 运行时资产清单记录了姿态、imageset 名称和地面锚点，供 Day 14 动画播放器直接接入。

## 当前质量检查

- 四姿态角色一致性：通过。
- 透明背景：通过；不再误伤眼睛与深色毛发。
- 画布：@3x 768 × 768，@2x 512 × 512，透明留白充足。
- 锚定：统一贴近画布底部，适合后续使用地面锚点定位。
- 边缘绿色溢色：通过连通色键与绿色残留清理。
- 独立动作层：四姿态共 48 个 imageset，完成。
- 闭眼变体：四姿态完成，可用于低频眨眼。
- 无签名 iOS Device Debug 构建：通过。

## 生成提示词摘要

使用内置 ImageGen，以 `warm-quiet-window-motion-study.png` 作为严格角色与画风参考；要求同一只成年柴犬、四姿态 2 × 2 母版、统一左侧暖光、绘本水彩与彩铅质感，并使用纯绿色背景供本地透明化处理。

完整母版与处理中间文件保存在 `design/production/day12/`，正式运行时资产保存在 `DogGo/Resources/Assets.xcassets/`。

## 完成定义

- [x] 四套基础姿态与角色一致性通过。
- [x] 身体、头、眼睛、耳朵、尾巴、腿、口鼻与阴影可独立寻址。
- [x] @2x、@3x PNG 均带透明通道并进入 Asset Catalog。
- [x] 文件命名、画布和地面锚点符合 Day 11 规格。
- [x] 资产目录可被 `actool` 编译并完成 Debug 构建。
